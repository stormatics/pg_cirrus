# inventory
# This file contains Ansible inventory configuration for all PostgreSQL nodes.

# Primary PostgreSQL server connection detaisls.
PRIMARY ansible_host=xxx.xxx.xxx.xxx ansible_connection=ssh ansible_user=postgres

# Standby PostgreSQL servers connection detaisls.
[STANDBY]
STANDBY1 ansible_host=xxx.xxx.xxx.xxx ansible_connection=ssh ansible_user=postgres
STANDBY2 ansible_host=xxx.xxx.xxx.xxx ansible_connection=ssh ansible_user=postgres
